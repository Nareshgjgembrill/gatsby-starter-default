import React from 'react';
import { render, act } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  // window.crypto = {
  //   subtle: {
  //     digest: () => '',
  //   },
  // };
  // act(() => {
  //   render(<App />);
  // });

  expect.anything();
});
